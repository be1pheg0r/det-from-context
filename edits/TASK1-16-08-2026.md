# TASK1-16-08-2026 — summary of edits

- Заменена привязка `slot == DETR query index` на one-to-one association по
  motion-aware IoU и cosine similarity.
- В протокол `ContextModule.read/write` добавлен текущий timestamp; wrapper
  передаёт `DetectionBatch.timestamp`.
- `MeMOTState` расширен полями времени истории, `missed`, `clock`, `evicted`;
  reset/detach учитывают все новые поля.
- Добавлены temporal encoding, constant-velocity prediction, expiry и полная
  очистка identity state при переиспользовании слота.
- Конфиг дополнен порогами association, весом appearance, `max_missed` и
  momentum движения.
- Тяжёлые tensor-проверки перенесены в `src/context_detection/smoke.py`;
  `tools/smoke_memot.py` и `tools/cli.py smoke` — тонкие точки входа.
- Новый `tests/test_memot_config.py` проверяет только Pydantic-контракты без
  прямого импорта torch.
- Удалено скрытие `agent_specs` и `CODEX.md` из `.gitignore`.
- Добавлен `docs/MeMOT.md`: устройство memory encoder и руководство по
  подключению learned-query/two-stage DETR.

Проверено: Ruff — clean; pytest — 48 passed; MeMOT smoke — 6 checks passed.
