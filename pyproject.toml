[project]
name = "context-detection"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "torchvision",
    "pycocotools",
    "numpy",
    "pillow",
    "matplotlib",
    "pydantic",
    "hydra-core==1.3.4",
]

[project.optional-dependencies]
# Версия ruff пиновая: она должна совпадать с rev в .pre-commit-config.yaml и
# с version в CI, иначе «локально зелено, в CI красно».
dev = ["pre-commit", "pytest", "ruff==0.16.3"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py312"
src = ["src", "tests", "tools"]
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = [
    "E", "W",  # pycodestyle
    "F",       # pyflakes
    "I",       # isort
    "UP",      # pyupgrade
    "B",       # bugbear
]

[tool.ruff.lint.per-file-ignores]
# Заготовки: сигнатуры с неиспользуемыми аргументами — это контракт, не мусор.
"src/context_detection/**" = ["ARG"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
